{
  "scripts": {
    "build": "wshcmx build --include-non-ts-files",
    "start": "wshcmx watch",
    "test": "npm install && npm run build && [[ $(find dist/* -name \"*.ts\" | wc -l) ]] && exit 1 || exit 0"
  },
  "dependencies": {
    "@wshcmx/cli": "file:../..",
    "@wshcmx/types": "^11.0.0-beta1"
  }
}
